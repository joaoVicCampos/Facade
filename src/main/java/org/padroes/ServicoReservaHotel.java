package org.padroes;

public class ServicoReservaHotel {
    public String reservar(String cidade, int noites) {
        return "Hotel reservado em " + cidade + " por " + noites + " noite(s).";
    }
}

